# Advanced Topics: Sending Email

**Status (docs):** The Statamic 6 docs page at `/advanced-topics/sending-email` currently returns a 404 with navigation only (no readable content via web_fetch).

## What to do instead
- Use Laravel mail configuration in `.env` (`MAIL_MAILER`, `MAIL_HOST`, etc.) and `config/mail.php`.
- Use Statamic’s Control Panel utilities to test email if available (Utility: email test).

## Sources
- https://statamic.dev/advanced-topics/sending-email
- https://laravel.com/docs/mail
